/**
 * Actions page.
 * Will display action register with personal and governance views.
 * Feature agent: actions-agent
 */
export default function ActionsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold tracking-tight">Actions</h1>
      <p className="text-muted-foreground mt-1">
        Action register — feature agent will build this out.
      </p>
    </div>
  );
}
