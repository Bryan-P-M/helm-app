/**
 * Workspace settings page.
 * Will display workspace configuration, member management, etc.
 * Feature agent: settings-agent
 */
export default function SettingsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold tracking-tight">Settings</h1>
      <p className="text-muted-foreground mt-1">
        Workspace settings — feature agent will build this out.
      </p>
    </div>
  );
}
