/**
 * Decisions page.
 * Will display decision log with rationale and participant tracking.
 * Feature agent: decisions-agent
 */
export default function DecisionsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold tracking-tight">Decisions</h1>
      <p className="text-muted-foreground mt-1">
        Decision log — feature agent will build this out.
      </p>
    </div>
  );
}
