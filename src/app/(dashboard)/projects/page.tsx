/**
 * Projects list page.
 * Will display project cards with RAG status, owner, and key metrics.
 * Feature agent: projects-agent
 */
export default function ProjectsPage() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold tracking-tight">Projects</h1>
      <p className="text-muted-foreground mt-1">
        Project portfolio — feature agent will build this out.
      </p>
    </div>
  );
}
